# Public origin of the deployed site. Drives canonical URLs, Open Graph tags,
# robots.txt and sitemap.xml — set this in production or they will point at
# http://localhost:3000.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
